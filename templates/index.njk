<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8"/>
        <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <title>moviefy</title>
    </head>

    <body>
        <ul>
            {% for movie in movies %}
                <li>
                    <a href="/player?title={{movie.title}}&path={{movie.path}}">{{movie.title }}</a>
                </li>
            {% endfor %}
        </ul>
    </body>
</html>