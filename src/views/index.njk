<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>moviefy</title>
		<link rel="stylesheet" href="public/css/bootstrap.css" />
	</head>
	<body class="p-3 bg-dark text-light">
		<p>{{ movies.length }} movies/s</p>
		<div class="list-group">
			{% for movie in movies %}
			<a href="/player?title={{ movie.title }}&path={{ movie.path }}&type={{ movie.type }}" class="list-group-item list-group-item-action {% if movie.supported == false %} list-group-item-warning {% endif %}">
				{{ movie.title }}
			</a>
			{% endfor %}
		</div>
	</body>
</html>
